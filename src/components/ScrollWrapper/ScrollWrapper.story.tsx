
import * as React from "react";

import { ScrollWrapper } from "./index";

export default {
  component: ScrollWrapper,
  title: 'ScrollWrapper',
};

export const default () => <ScrollWrapper  className="myClass" />;
