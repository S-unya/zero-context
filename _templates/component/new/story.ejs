---
to: src/components/<%= h.inflection.undasherize(name) %>/<%= h.inflection.undasherize(name) %>.story.tsx
---
<% const comp = h.inflection.undasherize(name) %>
import * as React from "react";

import { <%= comp %> } from "./index";

export default {
  component: <%= comp %>,
  title: '<%= comp %>',
};

export const default () => <<%= comp %>  className="myClass" />;
